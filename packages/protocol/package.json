{
  "name": "@notea/protocol",
  "version": "0.0.1",
  "private": true,
  "description": "Shared message contract and types between browser, orchestrator and workspace agent",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.6.5"
  }
}
