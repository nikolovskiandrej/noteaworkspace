{
  "name": "@notea/agents",
  "version": "0.0.1",
  "private": true,
  "description": "Provider registry, agent runtimes, worktree isolation, task state machine and serialized integration",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@notea/protocol": "0.0.1",
    "@notea/workspace-client": "0.0.1"
  },
  "devDependencies": {
    "@notea/workspace-agent": "0.0.1"
  }
}
